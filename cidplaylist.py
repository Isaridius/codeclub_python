def playlist(playlists):
    arr = playlist.split()
        hrs = int(arr[0])

# pseudocode
# so we have 3 playlists
# each playlist is an array of 3 items of hour minute and second
# first we will find out how many playlists tehre are
# then we figure out how many times we have to run the playlist function
# this playlist function 